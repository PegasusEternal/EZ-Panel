server {
  listen 80;
  server_name _;

  location / {
    proxy_pass http://ezpanel:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /healthz {
    proxy_pass http://ezpanel:5000/healthz;
  }
}

# Optional TLS termination example (uncomment & adjust paths)
# server {
#   listen 443 ssl;
#   server_name _;
#   ssl_certificate /etc/nginx/certs/ezpanel.crt;
#   ssl_certificate_key /etc/nginx/certs/ezpanel.key;
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_ciphers HIGH:!aNULL:!MD5;
#   location / {
#     proxy_pass http://ezpanel:5000;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;
#   }
#   location /healthz { proxy_pass http://ezpanel:5000/healthz; }
# }
